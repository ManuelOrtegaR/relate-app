import React from 'react';
import { Text, View } from 'react-native';
import { EvilIcons } from '@expo/vector-icons';

import globalStyles from '../App.styles.ts';

export const Rooms = () => {
  return (
    <View style={globalStyles.container}>
      <Text>Rooms here</Text>
    </View>
  );
};
